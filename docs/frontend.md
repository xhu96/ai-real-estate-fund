# Frontend

The React app is a control-plane prototype. It includes dashboard, analysis, runs, portfolio, research, and settings pages plus many diligence panels that can be wired to backend endpoints.

```bash
cd app/frontend
npm install
npm run dev
```
