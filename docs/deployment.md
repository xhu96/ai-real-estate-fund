# Deployment

The project includes Docker for the optional API:

```bash
docker compose up --build
```

Production deployment would need authentication, authorization, secrets management, observability, backups, database migrations, queue workers, rate limits, and security review.
